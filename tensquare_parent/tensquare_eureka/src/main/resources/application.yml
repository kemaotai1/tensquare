server:
  port: 6868
eureka:
  client:
    register-with-eureka: false  # 是否需要注册到Eureka
    fetch-registry: false  # 是否需要从Eureka发现其他组件
    service-url:  # Eureka调用地址
      defaultZone: http://127.0.0.1:${server.port}/eureka